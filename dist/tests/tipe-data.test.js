"use strict";
describe("Data Type", function () {
    it("should must desclare", function () {
        const name = "Anggy Triputra";
        const balanace = 100000;
        const isVip = true;
        console.log("name", name);
        console.log("balanace", balanace);
        console.log("isVip", isVip);
    });
});
